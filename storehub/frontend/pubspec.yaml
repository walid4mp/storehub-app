name: storehub_frontend
version: 1.0.0+1
description: StoreHub Flutter demo
publish_to: 'none'

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.2
  provider: ^6.1.2
  url_launcher: ^6.3.0

flutter:
  uses-material-design: true
